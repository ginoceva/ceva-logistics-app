name: Build Flet APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flet pandas openpyxl requests

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Linux Desktop Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build libgtk-3-dev mesa-utils pkg-config

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.4' # Version requested by flet-cli 0.82.0
          channel: 'stable'

      - name: Build APK
        env:
          PYTHONIOENCODING: utf-8
        run: |
          flet build apk --project "CEVA_Flow" --org "com.ceva.logistics"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ceva-logistics-apk
          path: build/apk/*.apk
